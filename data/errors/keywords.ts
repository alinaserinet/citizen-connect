import errors from './errors';

const errorKeywords = {
  network: errors.ERR_NETWORK,
  internet: errors.ERR_NETWORK,
  fetch: errors.ERR_NETWORK,
  unknown: errors.ERR_UNKNOWN,
};

export default errorKeywords;
